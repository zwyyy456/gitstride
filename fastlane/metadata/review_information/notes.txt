GitStride requires a GitHub.com account with access to at least one Project. In Settings > GitHub, choose Log In to GitHub, copy the displayed device code, open GitHub's device activation page, and approve the requested permissions. The App Store build uses OAuth only; it does not invoke GitHub CLI or include Sparkle. PR automation is optional and has a separate authorization flow. If a reviewer account has no Projects, create a personal Project on GitHub before testing the workspace.
